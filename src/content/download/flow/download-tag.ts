// Download source tags.
//
// We keep the user-facing labels in one place so strategy code, session state,
// and toast presentation all speak the same language.

export type DownloadTag = 'API' | 'DOM' | 'INTERCEPT' | 'BLOB';
